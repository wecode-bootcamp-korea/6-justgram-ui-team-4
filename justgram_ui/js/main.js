const inputComment = document.getElementsByClassName('inputNewComment');
const commentBtn = document.getElementsByClassName('newCommentBtn');
const commentList = document.getElementsByClassName('feedCommentlist');

commentBtn.addEventListener('click', () => {
  const content = inputComment.value;
  

  commentList  
})